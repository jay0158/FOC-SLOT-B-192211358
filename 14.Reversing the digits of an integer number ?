#include<stdio.h>
int main()
{ 
    int n,rev=0,rem;
    printf("Enter a number:");
    scanf("%d:,&n);
    while(n!=0)
    {
        rem = n%d;
        rev=rev*10+rem;
        n/=10;
     }
     print("reversed number:%d",rev);
     return 0;
}
